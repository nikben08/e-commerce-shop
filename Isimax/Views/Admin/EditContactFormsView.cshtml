@using Isimax.Models
@{
    Layout =  "~/Views/Shared/_DashboardLayout.cshtml";
}

<div class="container">
    <div class="row admin_panel-header">
        <div class="col-md-4 header-title">
            Isimax Administration
        </div>
        <div class="col-md-1 offset-md-7 logout-button">
            <form action="/Admin/Logout" method="post">
                <button>Çıkış Yap</button>
            </form>
        </div>
    </div>

    <div class="row dashboard-title">
        <div class="col-md-12">
           İletişim Formları
        </div>
    </div>

    <div class="row search-form">
        <form action="/Admin/SearchContactForm" method="post" >
            <div class="col-md-8">
                <div class="input-group">
                        <input type="text" name="query" class="form-control" placeholder="Search contact form" aria-label="Recipient's username" aria-describedby="button-addon2">
                        <button class="btn btn-outline-secondary" type="submit" id="button-addon2"><i class="fa-solid fa-magnifying-glass"></i></button>
                </div>
            </div>
        </form>
    </div>

    <div class="row data-list">
        <div class="col-md-12">
            <table class="table table-striped">
                <tr>
                    <th>Id</th>
                    <th>Name</th>
                    <th>Email</th>
                    <th>Tel</th>
                    <th>Tarih</th>
                    <th>Action</th>
                </tr>
                @foreach (var item in Model){
                    <tr>
                        <td>@item.Id</td>
                        <td>@item.Name</td>
                        <td>@item.Email</td>
                        <td>@item.Tel</td>
                        <td>@item.Date</td>
                        <td>
                            <button type="button" data-contactformid="@item.Id" class="btn btn-danger deleteContactForm">Delete</button>
                            <button type="button" data-contactformid="@item.Id" class="btn btn-primary viewContactForm">View</button>
                        </td>
                    </tr>
                }
            </table>
        </div>
    </div>


</div>